- name: display disk information
  hosts: local
  connection: local
  become: yes
  
  tasks:
  - name: command
    ansible.builtin.command: df -h 
    register: disk_size

  - name: check disk disk size 
    ansible.builtin.debug:
       msg: "{{ disk_size.stdout | replace('\n', ' ') }}"