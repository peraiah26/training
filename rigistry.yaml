- name: display disk information
  hosts: local
  connection: local
  become: yes
  command: df -h
  registry: disk_size
  tasks:
  - name: check disk disk size 
    ansible.builtin.debud:
      variable: disk_size