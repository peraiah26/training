- name: ansible-vault playbook
  hosts: local
  connection: yes
  become: yes
  vars_files:
    - 01-secratefile
  tasks:
  - name: ansible-vault
    ansible.builtin.debud:
      msg: "user name is: {{ USER }}, Passwd is: {{ PASSWD }}"